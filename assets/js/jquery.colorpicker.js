(function( $ ) {
    $(function() {
         
        // Add Color Picker to all inputs that have 'color-field' class
        $( '.lmpe-color-picker' ).wpColorPicker();
         
    });
})( jQuery );